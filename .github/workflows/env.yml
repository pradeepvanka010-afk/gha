name: env
on: workflow_dispatch
jobs:
   plan: 
       runs-on: self-hosted
       steps:
           - name: check repo
             uses: actions/checkout@v4
           - name: print
             run: echo index.html
